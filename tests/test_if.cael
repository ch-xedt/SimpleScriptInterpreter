let x = 1;
let w = x + 1;

if( w < x ){
  print(w + " is smaller than " + x);
}else{
  if(w = x){
    print("w as big as x");
  }else{
    let f = 2;
    if(w>x){
      let s = 12;
      print("w bigger than x"+f);
    }
  }
}

